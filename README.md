The following repository gives an example on how python can be used to perform ethical hacking techniques
The first file contains a python script that allows a user to enter files into the compiler and match the hashes of any set of files. This code can be used as a searching mechanism to to find an original file in a directory containing any number of random files.
The 
